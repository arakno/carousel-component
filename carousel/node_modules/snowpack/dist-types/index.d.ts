import { SnowpackConfig } from './config.js';
import { InstallTarget } from './scan-imports.js';
export interface DependencyLoc {
    type: 'JS' | 'ASSET';
    loc: string;
}
interface InstallOptions {
    hasBrowserlistConfig: boolean;
    isExplicit: boolean;
}
export declare function install(installTargets: InstallTarget[], { hasBrowserlistConfig, isExplicit }: InstallOptions, config: SnowpackConfig): Promise<boolean | undefined>;
export declare function cli(args: string[]): Promise<void>;
export {};
